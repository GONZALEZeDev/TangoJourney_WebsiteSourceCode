export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000'

// Gardez uniquement les routes nécessaires pour le site principal
export const API_ROUTES = {
  // Ajoutez ici les routes API dont vous avez besoin pour le site principal
}
